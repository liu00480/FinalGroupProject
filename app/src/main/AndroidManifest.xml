<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalgroupproject">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.audiodabaseapi.userAlbum"></activity>
        <activity android:name="com.example.audiodabaseapi.menu_album" />
        <activity android:name="com.example.audiodabaseapi.AlbumDetails" />
        <activity android:name="com.example.audiodabaseapi.song" />
        <activity android:name="com.example.audiodabaseapi.ArtistSearch" />
        <activity android:name="com.example.audiodabaseapi.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>